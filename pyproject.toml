[project]
name = "tt"
version = "1.0.0"
description = "Linux daily schedule manager with Pomodoro reminders"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.uv]
package = false
